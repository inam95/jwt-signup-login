# A quick run through of a sign-up login and protected routes in an node-api

## used packages:

    - jwtwebtokens
    - express
    - express-validator
    - bcrypt

## To run:

    - Clone the project
    - Run yarn install
    - Run yarn run start
